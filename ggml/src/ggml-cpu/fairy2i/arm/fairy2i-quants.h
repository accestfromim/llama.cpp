#pragma once

/*
 * Fairy2i ARM kernels live separately from legacy iFairy. The current
 * Fairy2i CPU production path is W2, so this header is intentionally narrow
 * until a native ARM tile64 vecdot or mul_mat path is added.
 */
